This a voting app where user can vote a specified candidate,check the list of candidates sorted by their vote counts.
Admin can add candidate,update cadidate's data , remove the candidate but can't vote.
